export enum S3TableColumn {
  CHECKBOX = "checkbox",
  NAME = "name",
  REGION = "region",
  ACCESS = "access",
  CREATION_DATE = "creation_date",
}

export enum S3ObjectsTableColumn {
  CHECKBOX = "checkbox",
  FILE_NAME = "fileName",
  SIZE = "size",
  LAST_MODIFIED = "lastModified",
}
